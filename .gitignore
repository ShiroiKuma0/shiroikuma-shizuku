*.iml
.gradle
/local.properties
/.idea/workspace.xml
/.idea/libraries
.DS_Store
/build
/captures
.externalNativeBuild
/.idea
/manager/signing.properties
/out
/signing.properties
/.kotlin
/sentry.properties
/.gemini/
/.mcp.json
/.claude/settings.local.json
/**/build/
# Fork: CLAUDE.md and .claude/skills/ are COMMITTED here (upstream ignores CLAUDE.md) — they are
# how this repo carries its own working rules. Only the machine-local settings file stays out.
GEMINI.md
/.ai/history/
.jules-key
jules-mcp.sh

# Local MCP config
.jules-key
jules-mcp.sh

# Downloaded CI/build logs
build_error*.log
build_app.log
build_failure.log
check.log
smoke-test.log
ktlint

# Fork: build artifact — copyCompatApkRelease/Debug generates this from the :compat module.
/manager/src/main/assets/compat.apk

# Fork: build artifact — generateBundledChangelog merges CHANGELOG-shiroikuma.md with the upstream
# commit delta into this asset, which the "What's New" dialog reads instead of the network.
/manager/src/main/assets/changelog.md
